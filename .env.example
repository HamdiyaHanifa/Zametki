# Скопировать этот файл в .env и вписать два значения из Supabase.
# Где взять: Supabase -> ваш проект -> Settings -> API
#
# ВАЖНО: файл .env не попадает на гитхаб (он указан в .gitignore).
VITE_SUPABASE_URL=сюда-адрес-проекта
VITE_SUPABASE_ANON_KEY=сюда-ключ-anon-public
